# ============================================================
# Strategic Market Intelligence Pipeline - Dependencies
# ============================================================

# --- Core Data & Storage ---
duckdb>=0.10.0          # In-process analytical database (Medallion Lake)
pydantic>=2.0.0         # Data validation & schema enforcement
pydantic-settings>=2.0.0 # Settings management via env vars

# --- HTTP & Feed Ingestion ---
feedparser>=6.0.10      # RSS/Atom feed parsing (default runnable connector)
requests>=2.31.0        # HTTP client for REST API connectors
httpx>=0.27.0           # Async-capable HTTP client (LexisNexis / Factiva)

# --- LLM & AI ---
anthropic>=0.27.0       # Anthropic SDK (claude-sonnet-4-5 extraction engine)

# --- Utilities ---
python-dotenv>=1.0.0    # .env file loader
tenacity>=8.2.0         # Retry logic with exponential back-off
rich>=13.7.0            # Beautiful console logging / progress bars

# --- Dev / Testing ---
pytest>=8.0.0
pytest-cov>=5.0.0
black>=24.0.0
ruff>=0.4.0
mypy>=1.10.0
